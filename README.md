Alteration of the library Node-rtsp-stream.
there are alterations when all the websocket clients disconect, the stream will stop.
There are some optimizations with a few functions and variables as well.